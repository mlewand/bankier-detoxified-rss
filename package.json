{
  "name": "bankier-detoxified-rss",
  "description": "RSS proxy that rewrites clickbait headlines into informative ones using LLMs.",
  "version": "0.0.2",
  "private": false,
  "license": "MIT",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.21",
    "@types/node": "^25.6.0",
    "@vitest/coverage-istanbul": "~3.2.0",
    "@vitest/coverage-v8": "~3.2.0",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.83.0"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "fast-xml-parser": "^5.6.0",
    "linkedom": "^0.18.12"
  }
}
